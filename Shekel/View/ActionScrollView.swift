// We are a way for the cosmos to know itself. -- C. Sagan

import SwiftUI

struct ActionScrollView: View {
    var body: some View {
        Text("Actions Scroll View")
            .underline()
//        ScrollView {
//            ForEach(actions) { action in
//                AnyView(createActionView(for: action)) // Function to create specific ActionView based on action type
//            }
//        }
    }
}
